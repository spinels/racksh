# frozen_string_literal: true

source "https://rubygems.org"

gem "rake"
# bundled with ruby but we need to add to Gemfile so "bundle exec" works
gem "test-unit" # https://github.com/test-unit/test-unit

gemspec
